{
  "name": "@repo/db-postgres",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "generate": "prisma generate",
    "db:pull:dev": "dotenv -e .env.development -- prisma db pull"
  },
  "dependencies": {
    "@prisma/client": "~6.2.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "dotenv-cli": "^11.0.0",
    "prisma": "~6.2.0"
  }
}