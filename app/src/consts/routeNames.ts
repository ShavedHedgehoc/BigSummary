export enum RouteNames {
  LOGIN = "/login",
  HOME = "/",
  LABORATORY = "/laboratory",

  REPORTS = "/reports",
  ADMIN = "/admin",
  FORBIDDEN = "/forbidden",
  USERS = "/admin/users",
  TECHNOLOGIST = "/technologist",
  PLANNER = "/planner",
  SUMMARY_UPLOAD = "/upload",
  REPORT1 = "/reports/1",
}
